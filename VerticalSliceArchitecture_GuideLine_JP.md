# Vertical Slice Architecture (VSA) 開発ガイドライン

あなたは **Vertical Slice Architecture (VSA)** および **Feature-First** の設計思想に基づいて開発を行います。コードを作成・変更する際は、以下のルールを厳格に順守してください。

## 1. ディレクトリ構造のルール
すべての機能（ドメイン）は、`src/features/` ディレクトリ配下に独立したスライス（機能フォルダ）として配置してください。
技術的な役割（Controller/Service等）ではなく、**ユーザーの行動や機能名**で切り出してください。

- ⭕ 良い例: `src/features/create-user/`, `src/features/download-invoice/`
- ❌ 悪い例: `src/controllers/`, `src/services/`, `src/models/`

## 2. スライス間の独立性ルール【最重要】
- **スライス間の直接参照・インポートは絶対禁止:**  
  `src/features/A` 配下のコードから、`src/features/B` 配下のコードを直接 `import` してはいけません。
- 複数のスライスで共有したいロジックがある場合は、`src/shared/` 配下に移動してください。
- スライス間で連携が必要な場合は、ドメインイベントを発行するか、親となるワークフロースライスを作成してください。

## 3. ファイルの同居（Colocation）ルール
機能に関連するファイルは、すべて同じスライスフォルダ内にまとめて配置してください。
- ハンドラー・実装ロジック (`<feature-name>.ts`)
- 入力バリデーション (`<feature-name>.schema.ts`)
- テストコード (`<feature-name>.test.ts`)

※ ルート直下の `tests/` や `schemas/` などにテストやスキーマを分離しないでください。

## 4. 型・DTOの独立管理
- リクエスト/レスポンスの型（DTO）やバリデーションスキーマを、他のスライスと共有・使い回ししないでください。
- 一見似ている型であっても、各スライスが自身の型定義を所有してください。

## 5. シンプルさの優先（YAGNI原則）
- 単純なCRUD機能に対して、不要なインターフェース（Interface）、リポジトリ（Repository）、抽象クラスを作らないでください。
- ロジックが複雑でない限り、1つのハンドラーファイル内にシンプルかつ透明性の高いコードを記述してください。